#include <stdio.h>

/**
* main - The entry point for the C program
*
* Return: Always 0 (success)
*/
int main(void)
{
	int i;

	for (i = 97; i < 123; i++)
	{
		_putchar(c);
	}
	_putchar(10);
	return (0);
}

