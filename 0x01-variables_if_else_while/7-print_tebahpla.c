#include <stdio.h>
/**
* main - Entry point for the C program
*
* Return: Always 0 (success)
*/
int main(void)
{
	int n;

	for (n = 122; n > 96; n--)
	{
		putchar(n);
	}
	putchar(10);
	return (0);
}

